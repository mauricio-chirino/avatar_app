# == Schema Information
#
# Table name: images
#
#  id         :bigint           not null, primary key
#  url_image  :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

one:
  url_image: string

two:
  url_image: string
